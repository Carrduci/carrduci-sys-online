import { CallfunctionPipe } from './callfunction.pipe';

describe('CallfunctionPipe', () => {
  it('create an instance', () => {
    const pipe = new CallfunctionPipe();
    expect(pipe).toBeTruthy();
  });
});
